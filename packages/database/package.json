{
  "name": "@romalms/database",
  "version": "1.0.0",
  "description": "",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "dev": "docker-compose up -d",
    "build": "tsc",
    "migrate:undo:all": "yarn build && npx sequelize db:migrate:undo:all",
    "migrate:all": "yarn build && npx sequelize db:migrate",
    "seeder:all": "yarn build && npx sequelize db:seed:all",
    "db:init": "yarn migrate:all && yarn seeder:all"
  },
  "author": "Romanize",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.2",
    "sequelize": "^6.21.6",
    "invariant": "^2.2.4"
  },
  "devDependencies": {
    "@romalms/tsconfig": "*",
    "eslint": "^7.32.0",
    "eslint-config-custom": "*",
    "sequelize-cli": "^6.4.1",
    "typescript": "^4.5.2"
  }
}
